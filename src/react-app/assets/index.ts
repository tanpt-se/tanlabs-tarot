const assetBase = import.meta.env.BASE_URL;

/** Static assets served from /public — no Vite bundle import */
export const NARRATOR_SPRITE = `${assetBase}characters/narrator-sprite.webp`;
export const CARD_BACK = `${assetBase}cards/card-back.svg`;
export const BRAND_LOGO = `${assetBase}brand/logo.svg`;
