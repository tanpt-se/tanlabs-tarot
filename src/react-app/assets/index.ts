const assetBase = import.meta.env.BASE_URL;

/** Static assets served from /public — no Vite bundle import */
export const CARD_BACK_CLASSIC = `${assetBase}cards/card-back-classic.svg`;
export const HOME_LOGO = `${assetBase}brand/home-logo.png`;
