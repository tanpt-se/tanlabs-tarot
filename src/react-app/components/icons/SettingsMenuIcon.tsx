import { Settings } from "lucide-react";

export function SettingsMenuIcon() {
	return (
		<Settings
			className="game-button__icon"
			aria-hidden="true"
			strokeWidth={1.75}
			absoluteStrokeWidth
		/>
	);
}
