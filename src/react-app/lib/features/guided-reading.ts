/** Guided spread — narrator-led reading with question, spread, and interpretation. */
export const GUIDED_READING_ENABLED = true;

/** Six-card in-depth spread — gated behind tokens (not yet implemented). */
export const GUIDED_SIX_CARD_ENABLED = false;

/** Three-card spread — temporarily disabled. */
export const GUIDED_THREE_CARD_ENABLED = false;
